Diagramy visual Paradigm, Dia

Praca s databazou:
    startNetworkServer -noSecurityManager

    CLI interface je: ij
        connect 'jdbc:derby://localhost:1527/calendar;user=user;password=user;create=true';

